name: mobile_flutter
description: Football Coaches App - Flutter scaffold
publish_to: "none"
version: 0.1.0+1
environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shimmer: ^3.0.0
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  provider: ^6.0.5
  flutter_secure_storage: ^8.0.0
  shared_preferences: ^2.0.17
  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.13
  flutter_local_notifications: ^16.3.2
  uuid: ^4.0.0
  intl: ^0.19.0
  equatable: ^2.0.0
  
  # 🎨 UI/UX Premium dependencies (if needed in future)
  # shimmer: ^3.0.0
  # fl_chart: ^0.66.0
  # lottie: ^3.0.0
  # flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/

# Web platform enabled - run with: flutter run -d chrome
